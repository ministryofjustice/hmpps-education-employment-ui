{% if matchedJobsResults.content.length >= 1 %}
  <h3 class="govuk-heading-s">{{ matchedJobsResults.totalElements }} {{ 'results' if  matchedJobsResults.totalElements > 1 else 'result' }}</h3>
  <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">
{% endif %}