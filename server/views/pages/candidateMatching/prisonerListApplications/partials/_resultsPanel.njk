{% if prisonerSearchResults.content.length >= 1 %}
  <h3 class="govuk-heading-s" id="results-count">{{ prisonerSearchResults.page.totalElements }} {{ 'result' if prisonerSearchResults.page.totalElements === 1 else 'results' }}</h3>
  <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">
{% endif %}